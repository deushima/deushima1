# QA — 2026-09-26

Base: origin/main 98b3578. Browser checks used Chromium on the connected machine.

## Fixes
- Keep the draggable navigation and expanded drawer within the viewport; release interrupted drags and respect reduced motion.
- Preserve node rich text, resized dimensions, connections, original layout and camera in autosave, using the same payload as export/import.
- Flush pending workspace changes on pagehide; restore the complete saved workspace without a delayed layout overwrite.
- Bound chat waits (12 seconds provider, 15 seconds client) and fall back to local portfolio responses.
- Keep chat keyboard focus inside the dialog and prevent late responses from focusing a closed dialog.
- Prevent duplicate contact submissions; reject malformed success responses and recover after timeouts.
- Add a default favicon for routes requesting /favicon.ico; update changed script/style cache versions.

## Verified
- Navigation at 320, 390, 768 and 1440 px; dragging near bottom and opening without clipping.
- About, Contact, all four Selected Work categories, fullscreen entry/exit and Selected Work inside fullscreen.
- Text creation/editing, Unicode, resize, duplication, node-to-node wiring, wheel zoom.
- Export/import round trip with connections and camera; invalid import leaves workspace unchanged.
- UI Link and image node creation; Save and two-step Import confirmation.
- Reload preserves node position, width/height, bold HTML and camera values exactly.
- Chat delayed response while closed, keyboard focus loop, normal local reply, 15-second timeout fallback.
- Contact validation with intercepted requests: double submission creates one request; malformed response reports error and retains input; success reports completion. No real message sent.
- Live /3deushima/ loads/rendering, all 18 tool sections open, ES/EN switching, 390 px layout has no horizontal overflow.
- JavaScript syntax checks and git diff whitespace validation.

## Limits
This is evidence for the tested flows, not a guarantee of zero bugs. No Safari/Firefox or physical touchscreen run, real contact delivery, external social embed playback, or 3D export/render parameter combinations were exhaustively tested.

